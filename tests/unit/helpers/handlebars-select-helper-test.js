import { handlebarsSelectHelper } from 'legislator-lookup/helpers/handlebars-select-helper';
import { module, test } from 'qunit';

module('Unit | Helper | handlebars select helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = handlebarsSelectHelper([42]);
  assert.ok(result);
});
