import { isParent } from 'legislator-lookup/helpers/is-parent';
import { module, test } from 'qunit';

module('Unit | Helper | is parent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isParent([42]);
  assert.ok(result);
});
